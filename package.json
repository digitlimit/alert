{
  "devDependencies": {
    "sass": "^1.85.1"
  },
  "scripts": {
    "sass": "sass --watch resources/scss/themes/tailwind:resources/css/themes/tailwind --no-source-map",
    "sass:build": "sass resources/scss/themes/tailwind:resources/css/themes/tailwind --no-source-map --style=compressed"
  }
}
